
function giftsInit(){
    let filmBar20 = new ProgressBar.Circle('#filmRoundProgress20-1', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FEB91F',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    filmBar20.animate(0.2);

    let filmBar202 = new ProgressBar.Circle('#filmRoundProgress20-2', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FEB91F',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    filmBar202.animate(0.2);

    var musicBar72 = new ProgressBar.Circle('#MusicRoundProgress72-1', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#8476FF',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        musicBar72.animate(0.72);

        var musicBar72 = new ProgressBar.Circle('#MusicRoundProgress72-2', {
            strokeWidth: 10,
            easing: 'easeInOut',
            duration: 1400,
            color: '#8476FF',
            trailColor: '#ECECEC',
            trailWidth: 10,
            svgStyle: {
                strokeLinecap: "round"
            }
            });
        
            musicBar72.animate(0.72);

    var educationBar4 = new ProgressBar.Circle('#EducationRoundProgress4-2', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#07C8E6',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        educationBar4.animate(0.04);

        var educationBar4 = new ProgressBar.Circle('#EducationRoundProgress4-1', {
            strokeWidth: 10,
            easing: 'easeInOut',
            duration: 1400,
            color: '#07C8E6',
            trailColor: '#ECECEC',
            trailWidth: 10,
            svgStyle: {
                strokeLinecap: "round"
            }
            });
        
            educationBar4.animate(0.04);
    
    var educationBar8 = new ProgressBar.Circle('#EducationRoundProgress8', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#07C8E6',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        educationBar8.animate(0.08);
}

function staffInit(){
     
    var filmBar84 = new ProgressBar.Circle('#filmRoundProgress84', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FEB91F',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    filmBar84.animate(0.84);

    let filmBar20 = new ProgressBar.Circle('#filmRoundProgress20', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FEB91F',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    filmBar20.animate(0.2); 

    let filmBar202 = new ProgressBar.Circle('#filmRoundProgress20-4', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FEB91F',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    filmBar202.animate(0.2); 

    var filmBar30 = new ProgressBar.Circle('#filmRoundProgress30', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FEB91F',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    filmBar30.animate(0.3);     

    var musicBar6 = new ProgressBar.Circle('#MusicRoundProgress6', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#8476FF',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });

        musicBar6.animate(0.06);

    var musicBar72 = new ProgressBar.Circle('#MusicRoundProgress72', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#8476FF',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        musicBar72.animate(0.72);

    let musicBar722 = new ProgressBar.Circle('#MusicRoundProgress72-4', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#8476FF',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        musicBar722.animate(0.72);

    

    var photoBar = new ProgressBar.Circle('#PhotoRoundProgress', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#8476FF',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        photoBar.animate(0.4);

    var educationBar10 = new ProgressBar.Circle('#EducationRoundProgress10', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#07C8E6',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    educationBar10.animate(0.10);

    var educationBar81 = new ProgressBar.Circle('#EducationRoundProgress8-1', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#07C8E6',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        educationBar81.animate(0.08);

    var educationBar82 = new ProgressBar.Circle('#EducationRoundProgress8-2', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#07C8E6',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    educationBar82.animate(0.08);

        var educationBar20 = new ProgressBar.Circle('#EducationRoundProgress20', {
            strokeWidth: 10,
            easing: 'easeInOut',
            duration: 1400,
            color: '#07C8E6',
            trailColor: '#ECECEC',
            trailWidth: 10,
            svgStyle: {
                strokeLinecap: "round"
            }
            });
        
            educationBar20.animate(0.2);    

    var drawingBar = new ProgressBar.Circle('#DrawingRoundProgress', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FD7123',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    drawingBar.animate(0.01);

    var drawingBar = new ProgressBar.Circle('#DrawingRoundProgress-2', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FD7123',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    drawingBar.animate(0.01);

    var drawingBar = new ProgressBar.Circle('#DrawingRoundProgress-3', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FD7123',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    drawingBar.animate(0.01);

    var drawingBar = new ProgressBar.Circle('#DrawingRoundProgress-4', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FD7123',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    drawingBar.animate(0.01);

    var techBar = new ProgressBar.Circle('#TechRoundProgress', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#7BC7B3',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    techBar.animate(0.06);

    var techBar = new ProgressBar.Circle('#TechRoundProgress-2', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#7BC7B3',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    techBar.animate(0.06);

    var techBar = new ProgressBar.Circle('#TechRoundProgress-3', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#7BC7B3',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    techBar.animate(0.06);

    var techBar = new ProgressBar.Circle('#TechRoundProgress-4', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#7BC7B3',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    techBar.animate(0.06);

    var autoBar = new ProgressBar.Circle('#AutoRoundProgress', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#DFE104',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        autoBar.animate(0.01);

    var autoBar = new ProgressBar.Circle('#AutoRoundProgress-2', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#DFE104',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        autoBar.animate(0.01);

    var autoBar = new ProgressBar.Circle('#AutoRoundProgress-3', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#DFE104',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        autoBar.animate(0.01);

    var autoBar = new ProgressBar.Circle('#AutoRoundProgress-4', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#DFE104',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        autoBar.animate(0.01);

    var codingBar = new ProgressBar.Circle('#CodingRoundProgress', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#FF5CF2',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
        codingBar.animate(0.01);

        var codingBar = new ProgressBar.Circle('#CodingRoundProgress-2', {
            strokeWidth: 10,
            easing: 'easeInOut',
            duration: 1400,
            color: '#FF5CF2',
            trailColor: '#ECECEC',
            trailWidth: 10,
            svgStyle: {
                strokeLinecap: "round"
            }
            });
        
            codingBar.animate(0.01);

        var codingBar = new ProgressBar.Circle('#CodingRoundProgress-3', {
            strokeWidth: 10,
            easing: 'easeInOut',
            duration: 1400,
            color: '#FF5CF2',
            trailColor: '#ECECEC',
            trailWidth: 10,
            svgStyle: {
                strokeLinecap: "round"
            }
            });
        
            codingBar.animate(0.01);


        var codingBar = new ProgressBar.Circle('#CodingRoundProgress-4', {
            strokeWidth: 10,
            easing: 'easeInOut',
            duration: 1400,
            color: '#FF5CF2',
            trailColor: '#ECECEC',
            trailWidth: 10,
            svgStyle: {
                strokeLinecap: "round"
            }
            });
        
            codingBar.animate(0.01);

    var swimmingBar = new ProgressBar.Circle('#SwimmingRoundProgress', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#5CD5FF',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    swimmingBar.animate(0.01);

    var swimmingBar = new ProgressBar.Circle('#SwimmingRoundProgress-2', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#5CD5FF',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    swimmingBar.animate(0.01);

    var swimmingBar = new ProgressBar.Circle('#SwimmingRoundProgress-3', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#5CD5FF',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    swimmingBar.animate(0.01);

    var swimmingBar = new ProgressBar.Circle('#SwimmingRoundProgress-4', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#5CD5FF',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    swimmingBar.animate(0.01);

    var dancingBar = new ProgressBar.Circle('#DancingRoundProgress', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#727272',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    dancingBar.animate(0.01);

    var dancingBar = new ProgressBar.Circle('#DancingRoundProgress-2', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#727272',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    dancingBar.animate(0.01);

    var dancingBar = new ProgressBar.Circle('#DancingRoundProgress-3', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#727272',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    dancingBar.animate(0.01);

    var dancingBar = new ProgressBar.Circle('#DancingRoundProgress-4', {
        strokeWidth: 10,
        easing: 'easeInOut',
        duration: 1400,
        color: '#727272',
        trailColor: '#ECECEC',
        trailWidth: 10,
        svgStyle: {
            strokeLinecap: "round"
        }
        });
    
    dancingBar.animate(0.01);
}

 // Number from 0.0 to 1.0